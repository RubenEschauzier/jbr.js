import type { IQueryResultDestination } from './IQueryResultDestination';
/**
 * A query results destination that writes to text files in a directory.
 */
export declare class QueryResultDestinationDirectory implements IQueryResultDestination {
    readonly path: string;
    /**
     * @param path Path to a directory.
     */
    constructor(path: string);
    write(queryId: number, query: string, results: string): Promise<void>;
}
