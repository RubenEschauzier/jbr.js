/**
 * Represents a single instance of validation parameters.
 */
export declare class QueryParameters {
    /**
     * A query identifier.
     */
    readonly queryIdentifier: string;
    /**
     * The parameters to fill into a query template.
     */
    readonly queryParameters: (string | number)[];
    /**
     * All expected results of the given query.
     * The outer array represents a solution sequence,
     * and the inner array represents a single solution mapping.
     */
    readonly results: QueryResult[][];
    constructor(
    /**
     * A query identifier.
     */
    queryIdentifier: string, 
    /**
     * The parameters to fill into a query template.
     */
    queryParameters: (string | number)[], 
    /**
     * All expected results of the given query.
     * The outer array represents a solution sequence,
     * and the inner array represents a single solution mapping.
     */
    results: QueryResult[][]);
}
export declare type QueryResult = string | number | QueryResult[];
