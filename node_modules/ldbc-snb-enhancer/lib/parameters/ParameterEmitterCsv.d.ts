import type { IParameterEmitter } from './IParameterEmitter';
/**
 * Emits parameters as CSV files.
 */
export declare class ParameterEmitterCsv implements IParameterEmitter {
    private readonly destinationPath;
    private readonly fileStream;
    private headerLength;
    constructor(destinationPath: string);
    emitHeader(columnNames: string[]): void;
    emitRow(columns: string[]): void;
    flush(): void;
}
