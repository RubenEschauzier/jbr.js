import type { IDataSelector } from './IDataSelector';
/**
 * A random data selector.
 */
export declare class DataSelectorRandom implements IDataSelector {
    private seed;
    constructor(seed: number);
    nextRandom(): number;
    selectArrayElement<T>(array: T[]): T;
}
