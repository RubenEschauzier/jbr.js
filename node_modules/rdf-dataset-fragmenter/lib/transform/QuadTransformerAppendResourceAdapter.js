"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.QuadTransformerAppendResourceAdapter = void 0;
const rdf_data_factory_1 = require("rdf-data-factory");
const DF = new rdf_data_factory_1.DataFactory();
/**
 * An abstract quad transformer that appends something to resources of the given type.
 */
class QuadTransformerAppendResourceAdapter {
    constructor(typeRegex) {
        this.type = new RegExp(typeRegex, 'u');
    }
    transform(quad) {
        const quads = [quad];
        // Append SCL policy to applicable resource type
        if (quad.subject.termType === 'NamedNode' &&
            quad.predicate.value === 'http://www.w3.org/1999/02/22-rdf-syntax-ns#type' &&
            quad.object.termType === 'NamedNode' && this.type.exec(quad.object.value)) {
            this.appendQuads(quad, quads);
        }
        return quads;
    }
}
exports.QuadTransformerAppendResourceAdapter = QuadTransformerAppendResourceAdapter;
//# sourceMappingURL=QuadTransformerAppendResourceAdapter.js.map