/// <reference types="node" />
import type { TransformCallback } from 'stream';
import { Transform } from 'stream';
import type * as RDF from '@rdfjs/types';
import type { IQuadTransformer } from './IQuadTransformer';
/**
 * A transform stream that runs quads through an array of transformers.
 */
export declare class QuadTransformStream extends Transform {
    private readonly transformers;
    constructor(transformers: IQuadTransformer[]);
    runTransformers(quad: RDF.Quad): RDF.Quad[];
    _transform(quad: RDF.Quad, encoding: BufferEncoding, callback: TransformCallback): void;
    _flush(callback: TransformCallback): void;
}
