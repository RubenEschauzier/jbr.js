import type * as RDF from '@rdfjs/types';
import { QuadTransformerTerms } from './QuadTransformerTerms';
/**
 * A quad transformer that replaces (parts of) IRIs.
 */
export declare class QuadTransformerReplaceIri extends QuadTransformerTerms {
    private readonly search;
    private readonly replacement;
    constructor(searchRegex: string, replacementString: string);
    protected transformTerm(term: RDF.Term): RDF.Term;
}
