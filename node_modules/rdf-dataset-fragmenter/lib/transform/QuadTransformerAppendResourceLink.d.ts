import type * as RDF from '@rdfjs/types';
import { QuadTransformerAppendResourceAdapter } from './QuadTransformerAppendResourceAdapter';
/**
 * A quad transformer that appends a link to resources of the given type.
 */
export declare class QuadTransformerAppendResourceLink extends QuadTransformerAppendResourceAdapter {
    private static readonly RDF_TYPE;
    private readonly predicate;
    private readonly link;
    private readonly linkType?;
    private readonly reverse?;
    private readonly linkRemoveTrailingSlash?;
    constructor(typeRegex: string, predicate: string, link: string, linkType?: string, reverse?: boolean, linkRemoveTrailingSlash?: boolean);
    protected appendQuads(original: RDF.Quad, results: RDF.Quad[]): void;
}
