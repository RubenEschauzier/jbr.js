import type * as RDF from '@rdfjs/types';
import type { IQuadMatcher } from '../quadmatcher/IQuadMatcher';
import type { IQuadTransformer } from './IQuadTransformer';
import type { ITermTemplate } from './termtemplate/ITermTemplate';
/**
 * A quad transformer that appends a quad to matching quads.
 */
export declare class QuadTransformerAppendQuad implements IQuadTransformer {
    readonly matcher: IQuadMatcher;
    readonly subject: ITermTemplate;
    readonly predicate: ITermTemplate;
    readonly object: ITermTemplate;
    readonly graph: ITermTemplate;
    constructor(matcher: IQuadMatcher, subject: ITermTemplate, predicate: ITermTemplate, object: ITermTemplate, graph: ITermTemplate);
    transform(quad: RDF.Quad): RDF.Quad[];
    protected appendQuads(original: RDF.Quad, results: RDF.Quad[]): void;
}
