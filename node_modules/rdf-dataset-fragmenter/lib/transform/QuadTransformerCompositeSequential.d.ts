import type * as RDF from '@rdfjs/types';
import type { IQuadTransformer } from './IQuadTransformer';
/**
 * Executes a collection of transformers in sequence.
 */
export declare class QuadTransformerCompositeSequential implements IQuadTransformer {
    private readonly transformers;
    constructor(transformers: IQuadTransformer[]);
    transform(quad: RDF.Quad, allowedComponent?: 'subject' | 'object'): RDF.Quad[];
    end(): void;
}
