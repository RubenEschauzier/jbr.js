import type * as RDF from '@rdfjs/types';
import type { QuadTermName } from 'rdf-terms';
import type { IQuadSink } from './IQuadSink';
/**
 * A quad sink that writes quads to a CSV file.
 */
export declare class QuadSinkCsv implements IQuadSink {
    private readonly file;
    private readonly columns;
    private fileStream;
    /**
     * @param file The file to write to.
     * @param columns The quad term names that will be serialized as columns.
     */
    constructor(file: string, columns: QuadTermName[]);
    close(): Promise<void>;
    push(iri: string, quad: RDF.Quad): Promise<void>;
}
