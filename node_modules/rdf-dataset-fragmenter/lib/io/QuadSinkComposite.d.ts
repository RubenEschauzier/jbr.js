import type * as RDF from '@rdfjs/types';
import type { IQuadSink } from './IQuadSink';
/**
 * A quad sink that combines multiple quad sinks.
 */
export declare class QuadSinkComposite implements IQuadSink {
    private readonly sinks;
    constructor(sinks: IQuadSink[]);
    push(iri: string, quad: RDF.Quad): Promise<void>;
    close(): Promise<void>;
}
