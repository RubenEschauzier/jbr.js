/// <reference types="node" />
import type { Readable } from 'stream';
import type * as RDF from '@rdfjs/types';
import type { IQuadSink } from '../io/IQuadSink';
import type { IFragmentationStrategy } from './IFragmentationStrategy';
/**
 * An abstract implementation of a fragmentation strategy.
 */
export declare abstract class FragmentationStrategyStreamAdapter implements IFragmentationStrategy {
    fragment(quadStream: RDF.Stream & Readable, quadSink: IQuadSink): Promise<void>;
    protected abstract handleQuad(quad: RDF.Quad, quadSink: IQuadSink): Promise<void>;
    protected flush(quadSink: IQuadSink): Promise<void>;
}
