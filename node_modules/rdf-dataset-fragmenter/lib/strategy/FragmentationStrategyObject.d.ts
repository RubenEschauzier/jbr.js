import type * as RDF from '@rdfjs/types';
import type { IQuadSink } from '../io/IQuadSink';
import { FragmentationStrategyStreamAdapter } from './FragmentationStrategyStreamAdapter';
/**
 * A fragmentation strategy that places quads into their object's document.
 */
export declare class FragmentationStrategyObject extends FragmentationStrategyStreamAdapter {
    private readonly blankNodeBuffer;
    constructor(eagerFlushing?: boolean);
    protected handleQuad(quad: RDF.Quad, quadSink: IQuadSink): Promise<void>;
    protected flush(quadSink: IQuadSink): Promise<void>;
}
