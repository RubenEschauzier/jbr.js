"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FragmentationStrategyObject = void 0;
const FragmentationBlankNodeBuffer_1 = require("./FragmentationBlankNodeBuffer");
const FragmentationStrategyStreamAdapter_1 = require("./FragmentationStrategyStreamAdapter");
/**
 * A fragmentation strategy that places quads into their object's document.
 */
class FragmentationStrategyObject extends FragmentationStrategyStreamAdapter_1.FragmentationStrategyStreamAdapter {
    constructor(eagerFlushing = true) {
        super();
        this.blankNodeBuffer = new FragmentationBlankNodeBuffer_1.FragmentationBlankNodeBuffer('object', 'subject', eagerFlushing);
    }
    handleQuad(quad, quadSink) {
        return __awaiter(this, void 0, void 0, function* () {
            // Only accept IRI subjects.
            if (quad.object.termType === 'NamedNode') {
                // If the subject is a named node, add the quad to the subject's document.
                yield quadSink.push(quad.object.value, quad);
                // Save the object in our blank node buffer, as it may be needed to identify documents for other quads.
                yield this.blankNodeBuffer.materializeValueForNamedKey(quad.subject, quad.object, quadSink);
            }
            yield this.blankNodeBuffer.push(quad, quadSink);
        });
    }
    flush(quadSink) {
        const _super = Object.create(null, {
            flush: { get: () => super.flush }
        });
        return __awaiter(this, void 0, void 0, function* () {
            yield _super.flush.call(this, quadSink);
            yield this.blankNodeBuffer.flush(quadSink);
        });
    }
}
exports.FragmentationStrategyObject = FragmentationStrategyObject;
//# sourceMappingURL=FragmentationStrategyObject.js.map