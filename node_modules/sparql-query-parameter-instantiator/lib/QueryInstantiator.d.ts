import type { QueryTemplateProvider } from './QueryTemplateProvider';
/**
 * Instantiates query providers a number of times.
 */
export declare class QueryInstantiator {
    private readonly providers;
    private readonly count;
    constructor(providers: QueryTemplateProvider[], count: number);
    protected instantiateProvider(provider: QueryTemplateProvider): Promise<void>;
    /**
     * Invoke all query providers.
     */
    instantiate(): Promise<void>;
}
