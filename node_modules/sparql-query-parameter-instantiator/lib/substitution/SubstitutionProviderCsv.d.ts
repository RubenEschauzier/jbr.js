import type { ISubstitutionProvider } from './ISubstitutionProvider';
/**
 * A subsitution provider for CSV files.
 */
export declare class SubstitutionProviderCsv implements ISubstitutionProvider {
    private readonly csvFilePath;
    private readonly columnName;
    private readonly separator;
    constructor(csvFilePath: string, columnName: string, separator?: string);
    getValues(): Promise<string[]>;
}
