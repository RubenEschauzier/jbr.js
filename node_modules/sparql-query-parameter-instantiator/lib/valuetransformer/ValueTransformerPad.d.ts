import type * as RDF from '@rdfjs/types';
import type { IValueTransformer } from './IValueTransformer';
/**
 * Pads strings until a given length.
 */
export declare class ValueTransformerPad implements IValueTransformer {
    private readonly paddingCharacter;
    private readonly paddingLength;
    private readonly start;
    constructor(paddingCharacter: string, paddingLength: number, start: boolean);
    transform(term: RDF.Term): RDF.Term;
}
