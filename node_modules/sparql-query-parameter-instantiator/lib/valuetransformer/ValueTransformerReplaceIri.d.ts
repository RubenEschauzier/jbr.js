import type * as RDF from '@rdfjs/types';
import type { IValueTransformer } from './IValueTransformer';
/**
 * Replaces parts of an IRI.
 */
export declare class ValueTransformerReplaceIri implements IValueTransformer {
    private readonly search;
    private readonly replacement;
    constructor(searchRegex: string, replacementString: string);
    transform(term: RDF.Term): RDF.Term;
}
